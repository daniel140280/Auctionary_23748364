meta {
  name: Login to a users account
  type: http
  seq: 3
}

post {
  url: http://localhost:3333/login
  body: json
  auth: inherit
}

headers {
  X-Authorization:     const item_id = parseInt(req.params.item_id, 10);
  
}

body:json {
  {
      "email": "daniel2@gmail.com",
      "password": "Querty123!"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
